import {SectionHeading} from "../../components/section-heading";

export function Projects() {
    return (
        <section className="vh-100 section-padding" id="projects">
            <SectionHeading heading='Projects'/>
        </section>
    )
}
